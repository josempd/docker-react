sudo: required
services:
    - docker

before_install:
    - docker build -t josempd/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true josempd/docker-react npm run test
language: generic